contract AuxFunc[Open] {
    state var counter: int = 0;

    constructor() {
        counter = 0;
    }
}

AuxFunc@Open(any) {
    // Expose a function inside the Open state to be imported and called
    external function func1(a: int, b: int) -> int {
        return a + counter;
    }
}