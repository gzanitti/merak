contract AuxFunc {
    state var counter: int = 0;

    constructor() {
        counter = 0;
    }

    external function func1(a: int, b: int) -> int {
        return a + counter;
    }

    entrypoint incrementCounter() {
        counter = counter + 1;
    }
}
